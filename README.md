# ZilanGroup Web Experience

A static PHP site with a simple session-based login and admin dashboard for managing featured users. Includes public member registration and admin-side user creation stored in `data/users.json`.
